import { Router } from 'express';

const router = Router();

// Placeholder for subscription routes
router.get('/usage', (req, res) => {
  res.json({ message: 'Subscription usage API - TODO: Convert from Next.js' });
});

export const subscriptionRouter = router;